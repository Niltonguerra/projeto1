services:
  scheduler-service:
    build:
      context: .
      target: development
    ports:
      - "3501:3501"
    volumes:
      - .:/app
      - cargo-cache:/usr/local/cargo/registry

volumes:
  cargo-cache: