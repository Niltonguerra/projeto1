info:
  name: testeDeConexão
  type: http
  seq: 1

http:
  method: GET
  url: "{{localUrl}}/users"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
